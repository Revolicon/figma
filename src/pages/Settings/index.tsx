import React from "react"

const Settings: React.FC = () => {
  return (
    <div
      style={{
        display: "flex",
        flexDirection: "column",
        gap: 10,
        padding: 12,
      }}
    ></div>
  )
}

export default Settings
